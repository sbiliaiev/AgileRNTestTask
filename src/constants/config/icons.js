import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';

export const userIcon = <Icon name="user" size={30} color="#000" />;
export const lockIcon = <Icon name="lock" size={30} color="#000" />;
